{
  "name": "rendering-cache-proxy",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --env dev --minify",
    "deploy:production": "wrangler deploy --minify"
  },
  "dependencies": {
    "hono": "^4.7.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250214.0",
    "wrangler": "^4.45.0"
  }
}
